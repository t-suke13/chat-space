.wrapper
  .chat-side
    .side-ber_user-name
      .top-items
        = current_user.name
        .top-items__name aa
      
        .top-items__icon
          =link_to "#", class: "logo-first" do
            = icon('fa','edit')
          =link_to edit_user_path(current_user) do
            = icon('fa','cog')

    .groups
      = link_to "#", class: "group" do
        .group__group-name
        .group__latest-message